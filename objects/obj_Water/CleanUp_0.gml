/// 
event_inherited();

surface_free(waterSurface);
surface_free(waterSurfaceRefractGlow);
surface_free(waterSurfaceRefractMask);
surface_free(waterSurfaceRefract);

surface_free(finalSurface);